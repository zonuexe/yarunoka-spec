{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://yarunoka.dev/schema/1.1/document.schema.json",
    "title": "Yrnk schedule document (version 1.1)",
    "description": "A document of the calendar-aware schedule DSL (Yrnk). The JSON Schemas under this base URI are the authoritative definition of the syntax; every language implementation is a copy of them. A document has three layers: reading directives (version, timezone) that declare how to interpret the document, external requirements (resolvers) that declare the names the document leaves to its host, and content (calendar, schedules). Annotations (label, description) may sit beside them and on each schedule: inert human-facing text that never takes part in validation of the rest, evaluation, or any query's answer. Constraints that JSON Schema cannot express are validated by implementations at parse time (see 'Constraints beyond the schema' in docs/specification.md): resolvability of every name — used and undefined means declared under 'resolvers', and no name is both a date_sets key and a declared resolver — presence of the calendar entries required by the calendar vocabulary, start < end and non-overlap of time windows, existence of dates and of the timezone name (IANA names only; fixed offsets are rejected), from < until, and presence of 'from' in schedules that use [\"every\", N, \"day\"]. Timezones with DST transitions are interpreted per RFC 5545 section 3.3.5.",
    "type": "object",
    "additionalProperties": false,
    "required": ["version", "timezone", "schedules"],
    "properties": {
        "label": {"$ref": "primitives.schema.json#/$defs/label"},
        "description": {"$ref": "primitives.schema.json#/$defs/description"},
        "version": {"const": "1.1"},
        "timezone": {"type": "string", "minLength": 1, "pattern": "\\S"},
        "resolvers": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {"$ref": "primitives.schema.json#/$defs/name"}
        },
        "calendar": {"$ref": "calendar.schema.json"},
        "schedules": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": {"$ref": "schedule.schema.json"}
        }
    }
}
